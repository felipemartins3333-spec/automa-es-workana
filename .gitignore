# Generated design-canvas bundle (rebuildable from the .dc.html sources + canvas.json
# via the /design skill's seed-canvas.mjs helper) — not committed, ~2.5MB minified.
faro-landing/faro-landing.html
